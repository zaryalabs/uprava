name: Uprava CI

"on":
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: [self-hosted, zarya-main, geo-eu, ci]
    steps:
      - name: Smoke
        run: true
