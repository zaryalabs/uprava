# Match: stat -c '%g' /var/run/docker.sock
UPRAVA_DOCKER_GID=990
# Generate once with: openssl rand -hex 32
# Keep it stable while state/toolhive contains encrypted credentials.
TOOLHIVE_SECRETS_PASSWORD=replace-with-a-random-production-secret
UPRAVA_TOOLHIVE_RUST_LOG=info
