# Uprava 0.2.0 — portable handoff

Updated: 2026-07-10

Branch: `0.2.0`

Implementation baseline: `2e4565e` plus the current uncommitted Milestone 1-7
checkpoint slice

Release state: `0.2.0-rc.6` automated candidate assembly in progress; not final

## Current point

The branch is a stable, checked implementation baseline with significant
quality-foundation work already integrated. It is not closeable using only the
four residual notes from the earlier private handoff: several original audit
steps remain partially implemented.

The authoritative remaining route is
[`0.2.0-completion-from-current.md`](../tmp-plans/0.2.0-completion-from-current.md).
It replaces the earlier worker/reviewer/manager execution protocol with one
sequential implementation stream. Small coherent changes and milestone gates
remain required, but subagent handoffs and private execution-unit tracking are
not.

The current pause point is also summarized in
[`0.2.0-checkpoint-2026-07-10-m7.md`](0.2.0-checkpoint-2026-07-10-m7.md).

The user owns the final live E2E and product review. Codex must finish the
implementation, automated clean-state checks and immutable RC handoff first.
After the user review, Codex fixes recorded defects and cuts final `0.2.0`.

## What is implemented

- Architecture, compatibility, separate state-slot and CI-lane decisions for
  `0.2.0`.
- Numbered Core SQLite migrations with checksums and incompatible-state guard.
- Core aggregate transaction and legal-transition improvements.
- Durable Core event-publication outbox with retry, replay and idempotency.
- Projection-completion boundary coupled to publication enqueue.
- Durable messages for `ProviderMessageCompleted`, `RuntimeError`,
  `ApprovalRequested` and `ApprovalResolved` coupled to the completion
  transaction and protected by `source_event_id` uniqueness.
- One Core event-ingest transaction now owns actor upsert, immutable event
  insert, cursor allocation, every normalized projection, stream-gap state,
  durable messages, projected marker and publication-outbox enqueue for all 19
  event kinds. Pending replay uses the stored envelope rather than duplicate
  input.
- Durable Core command-dispatch outbox with transactional enqueue, backfill,
  at-least-once retry accounting and terminal cleanup.
- Bounded generation-aware Core control registry with post-Hello activation,
  stale-generation protection, object ownership/transition validation, typed
  result echoes, frame/batch/output limits and queue rejection metrics.
- Canonical workspace UI routing now uses `/workspaces/:placement_id`; the old
  `/placements/:id` user workbench alias is removed.
- Workspace command execution now has a durable async resource path:
  `202 Accepted`, progress lookup, terminal result payloads, explicit cancel,
  auto-expiry and guarded waiter cleanup.
- Core terminal fan-out is routed through per-terminal bounded channels rather
  than one global terminal broadcast.
- Project/Placement convergence improvements and addressed workspace command
  waiters with durable result fallback.
- Argon2id password records and transactional enrollment audit.
- Protocol `v2` coordinated across Core, Node and Web.
- Versioned Node SQLite state with normalized registration, command cache,
  event outbox, runtime metadata, transcript, resume-reference and placement
  sequence tables.
- `NodeSupervisor` now owns Node main-loop lifecycle, control task shutdown and
  state-store actor join.
- Node state mutations now go through a bounded state-store actor with typed
  request/response messages.
- SQLite `snapshot_json` is now only a compatibility seed; normalized tables
  are authoritative for registration, command cache/results, event outbox,
  runtime metadata/transcripts/resume refs, placement sequences and counters.
- Node event-outbox retention is bounded by count, age and serialized bytes.
- Node control transport now separates the websocket reader, bounded writer and
  bounded command dispatcher.
- Node dispatch uses a priority cancellation queue, retryable busy result
  payloads, per-runtime/per-placement execution keys and bounded concurrency.
- Codex provider execution has runtime cancellation tokens, bounded
  stdout/stderr and activity retention, typed truncation evidence and
  incremental approval/resume parsing before evidence truncation.
- Workspace PTYs now live behind a daemon-owned terminal supervisor with
  bounded count/byte replay, explicit replay/stream gap evidence and joined
  close/shutdown task handling.
- Node owner-boundary mutation APIs, normalized reopen hydration, bounded
  command cache/outbox and reconnect replay.
- Command outcomes, event batches, ACK state and enrollment transitions
  persisted before external delivery at the completed owner boundaries.
- Persisted Node counters for reconnects, dropped events and heartbeat
  failures.
- Terminal outbound-route rebinding after Core control reconnect.
- SQLite online backup and read-only restore verification, deterministic
  temporary backup/restore rehearsal and guarded rollback rehearsal.
- Static non-root Core/Node/Web image policy checks.
- Deterministic Playwright golden tests isolated from host auth state and a
  passing real Codex smoke path.
- Protocol drift is now checked in `make dl`/`make c`: Rust enum wire literals,
  tracked Web literal constants and handwritten tagged-object discriminants are
  compared for 16 Web-facing roots.
- Web protocol aliases and validators now consume tracked generated literal
  constants from `apps/web/src/shared/protocol/literals.ts`.
- Web runtime validation now covers async workspace command resources, selected
  command/terminal HTTP responses, session SSE envelopes and terminal stream
  frames. Malformed high-risk payloads become visible client protocol errors or
  reload/error paths instead of silent casts.
- `UiBlock` is no longer a shared Rust/Web protocol DTO; it is a Web-local
  timeline view model.
- The request-time `ArtifactTree` API has been renamed to
  `SessionEvidenceProjection` at
  `/sessions/{session_thread_id}/evidence-projection`; projection node ids are
  deterministic and evidence-derived.
- Command addressing uses tagged `CommandTarget` variants instead of an
  optional identity bag; session legal actions use the shared
  `ActionCapability` enum.
- Known Core-to-Node command payloads use tagged `CommandPayload` variants;
  Core and Node reject kind/payload mismatch, and extensions require the
  explicit `CommandKind::Extension` boundary.
- Known Node capability values are tagged typed variants with an explicit
  extension escape hatch.
- Canonical Rust-generated high-risk Web fixtures are drift-checked and
  validated by the Web Zod suite.
- Inspector URLs use bounded versioned canonical reference keys rather than
  arbitrary encoded JSON.
- Workspace drafts preserve base/local/conflict state across route changes;
  all routes are lazy and the initial graph excludes Monaco/xterm under a
  checked 350 KiB gzip budget.

The detailed commit and residual ledger is in
[`0.2.0-implementation-ledger.md`](0.2.0-implementation-ledger.md).

## What remains

The ordered completion plan owns the detailed work. Its major open boundaries
are:

1. M7 is closed: command/event payloads, targets and capabilities are typed;
   canonical fixtures cover every runtime-validated root; session actions
   come from Core's authoritative projection.
2. Finish the remaining M8 workbench split and browser-level draft/conflict
   evidence; draft ownership and route-level lazy loading are implemented.
3. Complete public-ingress quotas, bounded non-blocking logging, metrics,
   correlation and optional OTLP behavior.
4. Finish Core, Node and Web module boundaries and dependency/docs hygiene.
5. Build and execute production images, then pass full clean-instance state,
   backup/restore and forced rollback rehearsals.
6. Assemble an immutable automated RC, hand it to the user for live E2E, fix
   evidence-backed defects and cut final `0.2.0`.

## Fresh verification evidence

One fresh full `make c` on 2026-07-10 passed:

- Node: 88 tests passed;
- Core library: 132 tests passed;
- Core binary: 1 test passed;
- Protocol: 8 tests passed;
- Logging: 1 test passed;
- Web: 56 tests in 18 files passed;
- Rust formatting, Clippy, dependency advisories, license/source policy,
  TypeScript lint/typecheck and production Web build passed.

The Rust dependency scan reports informational duplicate transitive versions;
it reports no advisory, ban, license or source failure.

The production Web build passes but confirms an open performance gate:

- initial JavaScript chunk: approximately 4.46 MiB, 1.17 MiB gzip;
- Monaco workers and xterm/workbench dependencies are reachable from the
  initial graph;
- Vite reports a chunk-size warning.

This is evidence for the Web lazy-loading milestone, not an accepted release
baseline.

Current checkpoint targeted evidence also includes:

- `uprava-protocol` has been mechanically split into focused modules while
  preserving the `lib.rs` public re-export surface;
- `scripts/protocol_check.py` now scans all protocol source modules after the
  split;
- `python3 scripts/protocol_check.py` passed for 16 Web-facing enum/discriminant
  roots;
- `cargo test -q -p uprava-protocol` passed;
- `cargo test -q -p uprava-server evidence_projection` passed;
- `npm --prefix apps/web run test -- http-client sse-client validators`
  passed 7 tests across 3 files;
- `npm --prefix apps/web run test -- App AgentProjectionPanel
  session-stream-cache` passed 11 tests across 4 files;
- `make l` passed.

Previously recorded targeted evidence also includes:

- deterministic Playwright golden suite passed;
- `make codex-smoke` passed Core health/auth, Node enrollment/inventory,
  provider capability and deterministic Web flow;
- temporary SQLite backup/restore and guarded rollback checks passed;
- production Docker runtime execution has not been verified because the
  previous environment had no available Docker daemon.

## Accepted constraints and known caveats

- P0-3 remains accepted risk: Codex runs with approvals/sandbox restrictions
  disabled under the trusted controlled-deployment boundary. Do not change or
  claim this posture as fixed without a scope decision.
- There is no in-place `0.1.x` data migration. Never point a `0.1.8` binary at
  `0.2.0` state or overwrite the retained `0.1.8` state/configuration.
- Current package/release metadata remains `0.1.8`; protocol and state slots
  are already the coordinated `0.2.0`/v2 boundary. Version metadata moves to
  `0.2.0-rc.6` after stable-path and cold-runner CI fixes.
- Event and command delivery are intentionally at-least-once; handlers must
  remain idempotent.
- Node's ephemeral localhost enrollment fixture is sandbox-sensitive and can
  also return HTTP 503 in the full suite. It has passed both full and isolated
  escalated runs, but the fixture remains a quality-gate reliability issue.
- Do not interpret static non-root image checks as runtime proof.
- Do not substitute automated golden/smoke tests for the user's final live
  E2E review.

## Resume on another machine

1. Clone/fetch the repository and switch to branch `0.2.0`.
2. Read the documents in the order listed in
   [`docs/polish/README.md`](README.md).
3. Confirm `git status --short --branch` and inspect any user changes before
   editing.
4. Run `make help`, `make init` if dependencies/hooks are not installed, then
   `make l`.
5. Run `make c` to establish the new machine baseline. If Docker is available,
   also establish the production image/runtime gate when that milestone is
   reached.
6. Preserve separate `0.1.8` and `0.2.0` Core/Node state roots. Do not copy
   ignored local databases, credentials or logs from the old machine.
7. Start with Milestone 1 of the completion plan. Work sequentially; add
   focused regression tests, run `make l` at each slice and `make c` at the
   milestone gate.
8. Update this handoff, the implementation ledger and RC checklist at
   meaningful milestone boundaries.

Local service commands and environment variables are documented in the
[development runbook](../en/runbooks/v01-local-dev.md). Routine operations go
through `make`.

## Handoff completion condition

This file stops being active when the final `0.2.0` release is recorded, the
quality-foundation and completion plans are marked implemented, and durable
decisions/evidence have been promoted to their canonical documents.
