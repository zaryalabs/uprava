# План имплементации Cortex 0.1

Статус: `draft`

Последнее обновление: `2026-06-17`

Scope: первая implementation release для продуктового среза V01.

Исходные документы:

- [`../en/v01.md`](../en/v01.md)
- [`../ru/v01.md`](../ru/v01.md)
- [`../en/architecture.md`](../en/architecture.md)
- [`../ru/architecture.md`](../ru/architecture.md)
- [`../en/tech-stack.md`](../en/tech-stack.md)
- [`../ru/tech-stack.md`](../ru/tech-stack.md)
- [`../en/design/001-distributed-architecture.md`](../en/design/001-distributed-architecture.md)
- [`../en/design/002-run-mode.md`](../en/design/002-run-mode.md)
- [`../en/design/003-distributed-runtime-coordination.md`](../en/design/003-distributed-runtime-coordination.md)
- [`../en/design/004-modular-ui-work-surface.md`](../en/design/004-modular-ui-work-surface.md)
- [`../en/design/005-dynamic-ui-from-agents.md`](../en/design/005-dynamic-ui-from-agents.md)

## Назначение

Этот документ превращает уже спроектированный продуктовый срез V01 в порядок
реализации для версии `0.1`.

`V01` - это scope продукта: **Distributed Agent Control Panel**. `0.1` - это
первая implementation release, в которой этот scope работает end-to-end в
trusted local/single-user или controlled development deployment.

План временный. Когда в ходе реализации появляются долговечные решения, их нужно
переносить в канонические документы. Этот файл не должен становиться постоянным
источником архитектуры.

## Release Contract 0.1

Версия `0.1` готова, когда пользователь может:

1. Запустить Core Backend и Web Control Panel локально.
2. Запустить как минимум один Node Daemon и enroll его в Core.
3. Увидеть в Web Control Panel node status, heartbeat age, capabilities,
   degraded state and active sessions.
4. Зарегистрировать или открыть project/workspace binding на выбранной node.
5. Запустить Codex-backed persistent interactive session для этого workspace.
6. Отправлять chat turns через Core-routed commands.
7. Получать ordered provider/runtime events в session view.
8. Detach и reattach к той же session.
9. Interrupt, stop and resume там, где это поддерживает provider adapter.
10. Перезагрузить UI и восстановить nodes, workspace bindings, sessions,
    messages, runtime state and recent event history из Core persistence.
11. Видеть stale/offline/error/resource warning states как UI state, а не как
    текст, спрятанный в chat.
12. Видеть явное trusted/local/non-production warning там, где UI открывает
    node pairing или remote control.

## Явно не входит в 0.1

В `0.1` не реализуем:

- production security hardening;
- multi-user auth, team RBAC, secrets management, hardened credentials;
- полноценный Tool Registry или Plugin Registry;
- MCP integration platform;
- Project Workspace Inspector, file tree, editor, terminal, diff/check surface;
- full visual artifact system or artifact gallery beyond lightweight
  session-local `artifact.tree` scaffolding;
- task-based sandbox runs или durable workflow engine;
- multi-agent orchestration;
- cloud node pool;
- external plugin ecosystem or arbitrary plugin JS in the main React tree;
- full provider-neutral parity beyond the Codex-first adapter;
- Next.js как обязательный app runtime;
- Tauri-specific domain logic.

## Рабочие implementation decisions

Эти решения достаточны, чтобы начать реализацию. Если реализация покажет, что
решение нужно поменять, обновляем этот план и переносим итоговую позицию в
канонические документы.

| Область | Решение для 0.1 |
| --- | --- |
| Backend | Rust, Tokio, Axum |
| Node Daemon | Rust, Tokio |
| Persistence | SQLite first |
| Web app | React 19, TypeScript, Vite SPA |
| Styling | Tailwind CSS v4, shadcn/ui conventions, lucide-react |
| Client state | TanStack Query plus local and URL state |
| UI snapshots | HTTP JSON APIs |
| Browser live events | SSE для session/event streams, если WebSocket не окажется явно проще |
| Core-to-Node control | outbound WebSocket/control channel from Node to Core |
| Core-to-Node presence | HTTP heartbeat from Node to Core, default около 5 секунд |
| First provider | Codex через минимальный Provider Adapter boundary |
| First deployment profile | trusted local/single-user or controlled development |
| Durable truth | Core persistence and event log, not browser state or provider process memory |

## Critical Open Decisions

Эти решения нужно закрыть до соответствующей стадии реализации.

| Решение | Нужно до | Default, если не закрыто |
| --- | --- | --- |
| SQLite access layer: SQLx, rusqlite или другой crate | Core persistence stage | SQLx, если compile-time workflow не мешает; иначе rusqlite для быстрого bootstrap |
| Codex runtime protocol: live process IO, provider resume command или adapter wrapper | Codex adapter stage | сначала fake provider; real Codex adapter остается единственным provider-specific module |
| UI live transport: SSE или WebSocket | Web session stream stage | SSE для browser events, WebSocket только для Node control |
| Workspace registration: explicit path или root scanning | Node workspace stage | explicit registration first |
| Same workspace active runtime: warning или hard block | coordination preflight stage | warning with explicit override in `0.1` |

## Architecture Slices

### Core Backend

Core - client-facing control plane.

Core владеет:

- node enrollment and registry;
- heartbeat projection and node status;
- project and workspace placement metadata;
- session threads, turns, messages, runtime projections;
- command intents and idempotency records;
- ordered event log and per-session projections;
- warning acknowledgements;
- API and live event streams for Web Control Panel;
- static delivery of the built web app, если это практично для `0.1`.

Core не должен:

- напрямую выполнять local files, shell или provider processes;
- требовать от browser прямого подключения к каждой node;
- считать provider process state долговечной truth.

### Node Daemon

Node Daemon - data plane на конкретной машине.

Node владеет:

- local daemon config and development credential;
- outbound registration, heartbeat and control channel;
- workspace path validation;
- minimal capability and health snapshot;
- minimal git/resource snapshot;
- provider process lifecycle;
- command execution and deduplication by `command_id`;
- provider event normalization into Cortex events;
- runtime lifetime policy and local process handles.

Node не должен:

- владеть global session history;
- обходить Core для client-visible commands;
- прятать важные provider actions только в raw terminal output.

### Web Control Panel

Web Control Panel - первый client. Он должен ощущаться как control panel, а не
как full IDE.

UI владеет:

- app shell and `Nodes -> Projects/Workspaces -> Sessions` tree;
- node pairing and diagnostics surfaces;
- project/workspace binding flow;
- session/chat surface;
- message, runtime event, status and approval rendering;
- lifecycle controls: start, attach, detach, interrupt, stop, resume;
- visible warning badges and trusted deployment messaging.

UI не должен:

- подключаться напрямую к Node Daemons;
- изобретать durable state, которого нет в Core;
- показывать future workspace/editor/terminal surfaces так, будто они уже
  входят в `0.1`.

## Data Model Baseline

Первая схема должна быть небольшой, но явной. Имена могут поменяться во время
имплементации, но границы ответственности должны сохраниться.

### Core Tables

- `nodes`: `node_id`, display name, enrollment state, presence state, optional
  sleep/power hint, last heartbeat, version, created/updated timestamps.
- `node_capabilities`: node id, capability key, version/value JSON.
- `node_enrollments`: short-lived pairing request, pairing code hash, status,
  expiry, claimed node id.
- `projects`: тонкая logical project identity, display name, optional repo id.
- `project_placements`: node-local workspace binding candidate with node id,
  project id, workspace path, git snapshot, last validation.
- `session_threads`: durable user work thread, project placement, run mode,
  runtime strategy, work contract, status, provider resume reference.
- `runtime_sessions`: live-or-recoverable runtime projection, provider,
  lifecycle state, timestamps, last runtime step, exit reason.
- `turns`: user turn sequence, status, linked messages and blocked request.
- `messages`: user, assistant, system, runtime and approval-visible messages.
- `commands`: command id, target node/runtime/session, command kind, status,
  dedupe key, payload, created/completed timestamps.
- `events`: event id, command id, runtime session id, session thread id, seq,
  kind, payload, happened timestamp.
- `approvals`: approval or user-input request, status, payload, response.
- `warning_acknowledgements`: actor, warning kind, affected resources, command
  id, timestamp.

### Node Local State

- daemon installation id and config;
- Core URL and development node credential;
- registered workspace roots or explicit workspace paths;
- provider process handles and local runtime metadata;
- recent command dedupe cache, persisted where needed for reconnect safety;
- provider-native session id or resume cursor, если доступен.

## Event And Command Contract

### Command Envelope

Каждая Core-to-Node command должна включать:

```text
command_id
kind
target_node_id
session_thread_id optional
runtime_session_id optional
project_placement_id optional
issued_at
payload
```

Initial command kinds:

- `StartRuntime`
- `ResumeRuntime`
- `SendTurn`
- `ResolveApproval`
- `InterruptRuntime`
- `StopRuntime`
- `ValidateWorkspace`
- `RefreshResourceSnapshot`

Правила:

- Core записывает command intent до dispatch.
- Node deduplicates by `command_id`.
- Commands never hidden inside heartbeat payloads.
- Если control channel недоступен, Core записывает pending dispatch и просит
  Node открыть control channel через heartbeat response.

### Event Envelope

Каждый Node-to-Core runtime event должен включать:

```text
event_id
command_id optional
runtime_session_id
session_thread_id
seq
kind
happened_at
payload
```

Initial event kinds:

- `runtime.starting`
- `runtime.ready`
- `runtime.running`
- `runtime.blocked`
- `runtime.expired`
- `runtime.resuming`
- `runtime.stopped`
- `runtime.error`
- `turn.started`
- `turn.completed`
- `turn.interrupted`
- `provider.output.delta`
- `provider.message.completed`
- `approval.requested`
- `approval.resolved`
- `coordination.warning_acknowledged`
- `workspace.validated`
- `resource.snapshot.updated`

Правила:

- `seq` monotonic per `runtime_session_id`.
- Core deduplicates by `event_id`.
- Core может detect gaps by `seq`.
- UI projection строится per `session_thread_id`.
- Global ordering across all nodes не нужен для `0.1`.

## Distributed Runtime Implementation Blueprint

Эта секция намеренно дублирует часть design docs в implementation form. Во
время разработки это базовый контракт для Core, Node and Web; дизайн-доки
нужны только для проверки направления.

### Main Runtime Flow

Golden path:

```text
Web command
-> Core API validates actor, placement, node and resource signals
-> Core records command intent
-> Core dispatches command to Node over control channel
-> Node acknowledges command receipt
-> Node executes through Provider Adapter
-> Node emits ordered events
-> Core persists events and updates projections
-> Web receives SSE events and refreshes affected query caches
```

Important invariants:

- browser never connects directly to Node;
- heartbeat negotiates channel state, but never carries runtime commands;
- command state is durable in Core before dispatch;
- event state is durable in Core before UI treats it as truth;
- Node may buffer events during short Core/channel outages, but Core remains the
  durable product history;
- command retry must not create duplicate turns, approvals or runtime actions;
- provider-specific details do not escape the Provider Adapter boundary.

### API Surface Baseline

Endpoint names can change during implementation, but the surface area should
stay close to this shape.

Client-facing Core HTTP:

```text
GET  /api/v1/health
GET  /api/v1/version
GET  /api/v1/inventory
GET  /api/v1/nodes
GET  /api/v1/nodes/{node_id}
POST /api/v1/node-enrollments
POST /api/v1/node-enrollments/{enrollment_id}/approve
POST /api/v1/nodes/{node_id}/revoke
POST /api/v1/project-placements/validate
POST /api/v1/sessions
GET  /api/v1/sessions/{session_thread_id}
GET  /api/v1/sessions/{session_thread_id}/messages
GET  /api/v1/sessions/{session_thread_id}/events?after_seq=...
GET  /api/v1/sessions/{session_thread_id}/stream
GET  /api/v1/sessions/{session_thread_id}/artifact-tree
GET  /api/v1/sessions/{session_thread_id}/agent-projection
POST /api/v1/sessions/{session_thread_id}/turns
POST /api/v1/sessions/{session_thread_id}/approvals/{approval_id}/resolve
POST /api/v1/runtime-sessions/{runtime_session_id}/interrupt
POST /api/v1/runtime-sessions/{runtime_session_id}/stop
POST /api/v1/runtime-sessions/{runtime_session_id}/resume
```

Node-facing Core HTTP/control:

```text
POST /api/v1/node/enrollment-requests
POST /api/v1/node/enrollment-claims
POST /api/v1/node/heartbeat
GET  /api/v1/node/control
```

Browser live stream:

- `GET /api/v1/sessions/{id}/stream` is SSE in `0.1` unless WebSocket becomes
  simpler during implementation.
- stream resumes by `Last-Event-ID` or explicit `after_seq`.
- client always loads an HTTP snapshot first, then applies events after the
  snapshot cursor.
- if a gap is detected, client invalidates the session queries and reloads the
  snapshot instead of guessing.

### State Machines

State enums should be encoded in the shared protocol/domain crate and tested as
transitions where practical.

Node enrollment:

```text
unregistered -> pending_user_approval -> registered
pending_user_approval -> expired
pending_user_approval -> rejected
registered -> revoked
```

Daemon presence:

```text
reachable -> stale -> offline
stale -> reachable
offline -> reachable
reachable -> revoked
stale|offline -> revoked
```

Node activity is not the same state machine as presence. A registered daemon
with fresh heartbeats and no active runtime is the normal baseline:

```text
reachable_idle     = reachable + active_runtime_count == 0
reachable_active   = reachable + active_runtime_count > 0
reachable_degraded = reachable + health/capability/resource degradation
```

Sleep/hibernation should be represented as a hint or reason, not as the only
source of truth:

```text
sleep_hint = unknown | awake | suspending | sleeping | woke
```

Reason: when a laptop sleeps, the daemon usually cannot heartbeat. Core can
infer `stale` or `offline`, but it can call the state `sleeping` confidently
only if Node reported a pre-sleep hint, a managed devbox/cloud provider reported
hibernation, or the daemon reports a wake reason on the next heartbeat. UI can
render `possibly sleeping` for stale personal nodes, but command routing should
still use reachability.

Control channel:

```text
closed -> requested -> connecting -> open
open -> closing -> closed
open -> lost -> requested
```

Command:

```text
recorded -> pending_dispatch -> dispatched -> acknowledged
acknowledged -> completed
acknowledged -> failed
recorded|pending_dispatch -> blocked
dispatched|acknowledged -> expired
```

Runtime session:

```text
starting -> ready -> running
running -> blocked
blocked -> running
running|blocked -> stopping -> stopped
running|blocked -> interrupted -> ready
running|ready|blocked -> stale
stale -> resuming -> running
running|ready|blocked|resuming -> error
running|ready|blocked|stale -> expired
expired -> resuming -> running
```

Session thread:

```text
created -> active
active -> detached
detached -> active
active|detached -> stopped
active|detached -> degraded
```

Turn:

```text
created -> dispatched -> running
running -> blocked_on_approval
blocked_on_approval -> running
running -> completed
running -> interrupted
created|dispatched|running -> failed
```

Hard blocks:

- node revoked or offline;
- workspace missing or not writable for write-capable runtime;
- required capability missing;
- command targets a stale runtime without a resume/reconnect path.

Warnings with persisted override:

- stale node;
- dirty workspace;
- same workspace active;
- same normalized repo/branch active elsewhere;
- branch behind upstream;
- optional capability missing.

### Control Channel Frames

The Node control channel should use small versioned JSON frames first. Binary
or compressed frames can come later only if needed.

Common frame fields:

```text
frame_id
protocol_version
kind
sent_at
payload
```

Core to Node frame kinds:

- `control.hello_ack`
- `command.dispatch`
- `command.cancel`
- `event_batch.ack`
- `config.update`
- `control.ping`

Node to Core frame kinds:

- `control.hello`
- `command.ack`
- `command.result`
- `event_batch`
- `node.diagnostic`
- `control.pong`
- `control.error`

Rules:

- Node sends `control.hello` with `node_id`, daemon version, protocol version,
  active runtime ids and last known event seq per runtime.
- Core rejects incompatible protocol versions before accepting commands.
- `command.dispatch` includes the full command envelope and expected target
  ids.
- Node sends `command.ack` after dedupe check and before provider execution.
- Node sends `command.result` for command-level completion/failure; runtime
  output still flows as events.
- Node sends `event_batch` in runtime seq order.
- Core persists accepted events and replies with `event_batch.ack`.
- Node keeps a bounded local outbox of unacked events until ack, reconnect or
  retention expiry.
- If local outbox retention is exceeded, Node emits the next possible
  `runtime.error` or `runtime.stale` event and Core marks the stream gap.

### Dispatch And Reconnect Rules

Core command dispatch:

1. validate placement, node state and resource warnings;
2. create `command_id`;
3. persist `commands` row as `recorded`;
4. write warning acknowledgement events if user overrode warnings;
5. mark command `pending_dispatch`;
6. if channel open, send `command.dispatch`;
7. if channel closed, set `channel_needed` for node and wait for heartbeat;
8. on `command.ack`, mark `acknowledged`;
9. on terminal command result or terminal runtime event, mark `completed` or
   `failed`.

Node reconnect:

1. Node heartbeats with `control_channel_open = false`;
2. Core heartbeat response says `open_control_channel = true` if work is
   pending or active;
3. Node opens outbound control channel;
4. Node sends active runtime ids, command dedupe cursor and last emitted seqs;
5. Core sends any pending commands whose targets are still valid;
6. Node retransmits unacked event batches;
7. Core deduplicates events by `event_id` and validates per-runtime `seq`;
8. UI receives normal events or a projection reload signal.

Duplicate handling:

- same `command_id` on Node returns previous command status/result where
  possible;
- same `event_id` in Core is ignored after verifying compatible payload;
- same `seq` with different `event_id` is a stream conflict and marks runtime
  degraded;
- gap in `seq` marks runtime/session projection as `degraded` until snapshot
  repair or explicit resume.

### Projection Rules

Core should maintain projections as read models, not as separate truth.

Minimum projections:

- inventory tree: nodes, placements, active sessions and warning badges;
- node detail: heartbeat age, capabilities, diagnostics, active runtime summary;
- placement detail: workspace validation, git/resource snapshot, start
  eligibility;
- session detail: session thread, runtime state, messages, approvals, warnings,
  recent events;
- runtime summary: lifecycle state, provider, last step, resume support,
  degraded reason.
- artifact tree: session-local index of turns, messages, approvals, warnings,
  runtime events, resource snapshots and artifact placeholders.
- agent projection: compact agent-readable session state and resume context
  rebuilt from Core persistence.

Messages are derived from events plus stored user turns:

- user turn creates a user message immediately after Core accepts command;
- `provider.output.delta` updates an in-progress assistant message;
- `provider.message.completed` finalizes assistant message content;
- `approval.requested` creates an approval-visible message/card;
- `runtime.*`, `workspace.*` and `resource.*` events are rendered as compact
  timeline/status blocks, not as assistant text.

### Node Implementation Modules

Node Daemon should stay split by responsibility:

```text
config          load/save daemon config and node credential
enrollment      request code, claim credential, revoke local state
heartbeat       periodic status and channel negotiation
control         websocket lifecycle, frame send/receive, reconnect
workspaces      explicit path validation and resource snapshots
commands        command dedupe and dispatch to runtime manager
runtimes        runtime process handles and lifecycle state
providers       Provider Adapter trait, fake provider, Codex provider
event_outbox    bounded local event buffering and ack handling
diagnostics     health/resource summaries and safe debug output
```

Node local persistence can be small and file-based in `0.1`, but it should have
a clear owner and schema. It must store at least daemon config, node credential,
recent command dedupe records and unacked event outbox records needed for
reconnect safety.

### Distributed Test Matrix

The implementation should add integration tests or smoke scenarios for:

- enrollment approval and claim;
- heartbeat reachable idle -> stale -> offline -> reachable idle;
- reachable idle -> reachable active -> reachable idle as runtimes start/stop;
- sleep hint reported before daemon sleep and cleared/reconciled on wake;
- control channel requested from heartbeat response;
- command pending while channel is closed, then dispatched after reconnect;
- duplicate command delivery does not duplicate turn execution;
- duplicate event delivery does not duplicate messages;
- event seq gap marks projection degraded;
- Core restart while Node/runtime is active;
- Node restart with active or recoverable runtime metadata;
- stale/offline node blocks new turns but keeps history readable;
- warning override creates `coordination.warning_acknowledged`;
- provider crash maps to `runtime.error` and user-safe UI state.

## Web UI Implementation Blueprint

V01 UI is a control panel with an internal extension architecture. It should not
ship external plugins or generated dynamic UI, but the first implementation
must be structured around surfaces, blocks, references, commands and context so
future work does not require reshaping the app.

### Frontend Source Shape

Baseline `apps/web/src` shape:

```text
app/
  App.tsx
  routes.tsx
  providers.tsx
  shell/
shared/
  api/
    http-client.ts
    sse-client.ts
    query-keys.ts
  protocol/
    generated-or-handwritten-types.ts
  ui/
    button.tsx
    badge.tsx
    dialog.tsx
    dropdown-menu.tsx
    textarea.tsx
    toast.tsx
  lib/
features/
  agent-projection/
  artifacts/
  inventory/
  nodes/
  placements/
  sessions/
  runtime/
  approvals/
  warnings/
workbench/
  surfaces/
  blocks/
  commands/
  references/
  navigation/
  inspector/
```

Rules:

- `shared/api` owns Core HTTP/SSE mechanics and never imports React components.
- `features/*` own product-specific queries, mutations and components.
- `workbench/*` owns reusable surface/block/command/reference infrastructure.
- `app/shell` composes routes, tree, main surface and detail/inspector area.
- UI components from shadcn conventions live in `shared/ui` and are adapted
  locally instead of being treated as opaque external widgets.
- feature components call Core APIs only through feature hooks/mutations.
- no direct Node URL, websocket or credential appears in frontend code.

### Route Map

Initial routes:

```text
/                              redirect to /nodes or last opened session
/nodes                         node overview
/nodes/:nodeId                 node detail
/nodes/:nodeId/placements/new  register/validate workspace
/placements/:placementId       placement detail and start session action
/sessions/:sessionThreadId     session/chat surface
/settings/runtime              minimal local/trusted profile diagnostics
```

The left inventory tree remains visible on desktop routes. On narrow screens it
can become a drawer, but route identity should stay the same.

### Surfaces

Core surfaces for `0.1`:

| Surface id | Purpose |
| --- | --- |
| `app.shell` | App frame, top status, trusted profile warning. |
| `inventory.tree` | `Nodes -> Projects/Workspaces -> Sessions`. |
| `node.detail` | Node status, heartbeat, capabilities, diagnostics, revoke. |
| `placement.detail` | Workspace path, git/resource signals, validation, start. |
| `session.header` | Breadcrumb, node/project/runtime/provider/status badges. |
| `session.timeline` | Messages, runtime events, approvals and warnings. |
| `session.composer` | User turn input and send state. |
| `session.controls` | Attach, detach, interrupt, stop, resume. |
| `artifact.tree` | Lightweight tree/index of session artifacts and important refs. |
| `agent.projection` | Compact current state projection for UI/debug and provider context. |
| `inspector.stack` | Reserved detail stack for selected references. |

V01 does not need a full workspace inspector, file tree, editor, terminal or
artifact gallery. It does need a lightweight `artifact.tree` surface as an
index of structured outputs, approvals, warnings, resource snapshots and future
artifact refs. The app should still reserve an `inspector.stack` slot so future
reference/detail surfaces have a stable place to mount.

### Internal Block Contract

Session timeline and future surfaces should render through an internal block
registry, not through one hardcoded transcript component.

Minimal TypeScript shape:

```ts
type CortexRef =
  | { kind: "node"; nodeId: string }
  | { kind: "placement"; placementId: string }
  | { kind: "session"; sessionThreadId: string }
  | { kind: "runtime"; runtimeSessionId: string }
  | { kind: "turn"; turnId: string }
  | { kind: "message"; messageId: string }
  | { kind: "block"; blockId: string }
  | { kind: "artifact"; artifactId: string }
  | { kind: "event"; eventId: string; runtimeSessionId: string; seq: number }
  | { kind: "command"; commandId: string }
  | { kind: "approval"; approvalId: string }
  | { kind: "warning"; warningKind: string; commandId?: string };

type UiBlock = {
  blockId: string;
  type: string;
  schemaVersion: number;
  surfaceId: string;
  primaryRef: CortexRef;
  parentRef?: CortexRef;
  children?: UiBlock[];
  traceRefs?: CortexRef[];
  data: unknown;
  actions?: string[];
  fallbackText?: string;
};
```

Initial core block types:

- `core.inventory-tree`;
- `core.artifact-tree`;
- `core.agent-chat`;
- `core.user-message`;
- `core.assistant-message`;
- `core.provider-output-stream`;
- `core.runtime-event`;
- `core.warning`;
- `core.error`;
- `core.approval-request`;
- `core.workspace-validation`;
- `core.resource-snapshot`;
- `core.agent-projection-summary`;
- `core.unknown`.

Renderer rules:

- renderer receives typed block data and current context;
- renderer may show actions, but it does not execute privileged behavior
  directly;
- actions go through the command registry;
- unknown block types render `core.unknown` with safe metadata and raw payload
  only if safe;
- raw provider text is treated as content, not as executable HTML;
- markdown rendering is allowed for assistant text, sanitized, and isolated
  from command execution.

Universal block rules:

- every visible block must have `blockId`, `type`, `schemaVersion`, `surfaceId`
  and `primaryRef`;
- block types are resolved through a registry, not `switch` statements spread
  across feature components;
- a renderer declares allowed surfaces, supported schema versions, actions and
  fallback behavior;
- nested blocks are allowed only where the surface expects them, such as
  `artifact.tree` and future reports;
- block descriptors can be stored or reconstructed from Core projections;
- adding a new internal block type should require registering a descriptor,
  renderer and tests, not editing the whole shell;
- external plugin-provided renderers remain out of scope for `0.1`, but the
  internal API should resemble a future contribution point.

Minimal renderer registration shape:

```ts
type BlockRendererRegistration = {
  type: string;
  supportedSchemaVersions: number[];
  allowedSurfaces: string[];
  render: React.ComponentType<BlockRendererProps>;
  fallback: React.ComponentType<BlockFallbackProps>;
  getActions?: (block: UiBlock, context: WorkbenchContext) => string[];
  getNavigableObjects?: (block: UiBlock) => NavigableObject[];
};
```

### Artifact Tree And Agent Projection

`0.1` should not build the full visual artifact system, but it should avoid a
chat-only model by shipping two small read models.

#### Artifact Tree

The artifact tree is a lightweight session-local index, not a full artifact
gallery. It groups important visible objects so the session can scale beyond a
linear transcript.

Initial artifact tree nodes:

- session root;
- turns;
- assistant messages;
- approval requests;
- warnings and warning acknowledgements;
- runtime lifecycle/error events;
- workspace validation results;
- resource snapshots;
- provider resume/degraded-resume records;
- future artifact placeholders when Core receives an unknown structured
  artifact/event.

Rules:

- every tree node has a `CortexRef`;
- selecting a node opens the inspector stack;
- tree nodes can point back to timeline blocks and source events;
- unknown artifact-like payloads are indexed as metadata plus safe fallback,
  not rendered as executable UI;
- this does not include file tree, editor, terminal, full diff view or artifact
  gallery.

#### Agent Projection

Agent projection is the minimal agent-readable read model of current session
state. It is not agent memory and not a hidden prompt transcript. It is a
structured snapshot Core can show in diagnostics, pass to a provider adapter as
resume context, and later expose to agents/tools as context.

Minimum shape:

```text
agent_projection
  session_thread_id
  project_placement
  runtime_summary
  current_turn optional
  pending_approvals
  active_warnings
  recent_turn_summaries
  recent_message_refs
  artifact_tree_summary
  available_block_types
  available_commands
  resume_context
  generated_at
```

Rules:

- projection is rebuilt from Core persistence and event log;
- projection is safe to show in UI diagnostics;
- projection excludes credentials, raw environment, secrets and unredacted
  provider payloads;
- provider adapter may use projection for degraded resume or explicit resume
  context;
- projection should say what is unknown or stale instead of inventing state;
- future agent-facing capability discovery should extend this shape rather than
  adding a parallel context model.

### Command Registry

V01 should implement an internal command registry even before external plugins.

Command shape:

```ts
type UiCommand = {
  id: string;
  title: string;
  icon?: React.ComponentType;
  when?: (context: WorkbenchContext) => boolean;
  run: (context: WorkbenchContext) => Promise<void>;
};
```

Initial commands:

- `node.createEnrollment`;
- `node.approveEnrollment`;
- `node.revoke`;
- `placement.validate`;
- `session.start`;
- `session.attach`;
- `session.detach`;
- `session.sendTurn`;
- `runtime.interrupt`;
- `runtime.stop`;
- `runtime.resume`;
- `approval.resolve`;
- `warning.acknowledge`;
- `reference.openInInspector`;
- `reference.copy`;

Rules:

- commands call Core API mutations, not Node;
- commands are enabled/disabled from context keys: selected node, selected
  placement, selected session, runtime state, node state, warning severity and
  trusted profile;
- toolbar buttons, timeline block actions and future command palette use the
  same command ids;
- every command that maps to Core runtime behavior returns or is linked to a
  `command_id`.

### Data Fetching And Live Updates

TanStack Query owns server state. Suggested query keys:

```text
["inventory"]
["nodes"]
["node", nodeId]
["placement", placementId]
["session", sessionThreadId]
["session", sessionThreadId, "messages"]
["session", sessionThreadId, "events", cursor]
["session", sessionThreadId, "artifact-tree"]
["session", sessionThreadId, "agent-projection"]
```

Rules:

- route load fetches HTTP snapshot first;
- active session route opens SSE after snapshot is loaded;
- SSE event updates specific query caches through projection helpers;
- if projection helper cannot apply event safely, invalidate and refetch;
- mutation success should prefer returned Core snapshot/command id over
  optimistic local truth;
- pending user turn can be shown immediately after Core accepts `SendTurn`, but
  final message state comes from events/projection.

Projection helpers should be pure functions with Vitest coverage:

- apply node heartbeat/status event to inventory;
- apply workspace validation/resource event to placement;
- apply provider delta/completed message to session timeline;
- apply runtime lifecycle event to header/status;
- apply approval requested/resolved to timeline;
- apply event/message/resource changes to artifact tree;
- rebuild or refetch agent projection when session/runtime state changes;
- detect missing seq and request snapshot reload.

### Layout And Component Inventory

Use a stable control-panel layout:

```text
top bar: product/profile/status
left: inventory tree
center: selected node/placement/session surface
right: inspector stack placeholder/detail
bottom: no global bottom panel in 0.1
```

Initial components:

- `AppShell`;
- `InventoryTree`;
- `NodeStatusBadge`;
- `HeartbeatAge`;
- `CapabilityList`;
- `NodeEnrollmentDialog`;
- `NodeDiagnosticsPanel`;
- `WorkspacePlacementForm`;
- `ResourceBadgeList`;
- `WarningGate`;
- `SessionHeader`;
- `SessionTimeline`;
- `TimelineBlockRenderer`;
- `ArtifactTree`;
- `AgentProjectionPanel`;
- `BlockRendererRegistry`;
- `ChatComposer`;
- `LifecycleControls`;
- `ApprovalCard`;
- `RuntimeEventItem`;
- `InspectorStack`;
- `TrustedProfileBanner`;

Design constraints:

- keep visual density appropriate for a control panel;
- use cards only for repeated entities, dialogs and clearly framed controls;
- use badges for status/warnings, icon buttons for compact lifecycle controls,
  and forms for node/workspace setup;
- do not show future workspace/editor/terminal panels as if implemented;
- show idle/active/degraded/stale/offline/error/resource states outside
  assistant text;
- make long paths, node names and provider output wrap or truncate without
  breaking layout.

### References And Inspector

Every important visible object should have a `CortexRef`. For `0.1`, refs are
mostly used for internal actions and the inspector placeholder.

Initial inspector details:

- node ref: status, heartbeat, capabilities, diagnostics summary;
- placement ref: workspace path, validation result, resource badges;
- session ref: runtime summary and recent events;
- event ref: kind, seq, happened_at, command id and safe payload;
- command ref: kind, status, target ids and timestamps;
- approval ref: request payload and resolution state;
- warning ref: warning kind, affected resources and acknowledgement.

The inspector can start as a right-side drawer/panel. It should be implemented
as a stack of refs, not as one hardcoded modal, so future "go to cause" and
artifact details have somewhere to land.

### Navigation And Accessibility Baseline

Full semantic keyboard navigation can come later, but V01 should not block it.

Minimum:

- stable focus management for dialogs, drawers, tree and composer;
- roving tabindex or equivalent for inventory tree;
- visible focus states;
- keyboard-accessible lifecycle and approval actions;
- `Esc` closes dialogs/inspector before affecting composer input;
- timeline blocks expose labels and actions without relying on color alone.

Optional if time allows:

- command palette backed by the command registry;
- `j/k` movement inside timeline when composer is not focused;
- `Enter` opens selected ref in inspector.

### UI Test Matrix

Web tests should cover:

- route renders from mocked Core snapshots;
- inventory tree status badges for idle, active, degraded, stale, sleeping hint,
  offline and error;
- workspace validation form success/failure;
- session timeline block rendering for all initial block types;
- artifact tree renders indexed session objects and opens refs in inspector;
- agent projection panel renders safe current-state summary;
- provider delta followed by completed message;
- approval request and resolve flow;
- lifecycle controls enabled/disabled by runtime/node state;
- SSE gap triggers snapshot reload;
- unknown block fallback;
- trusted/local warning appears in enrollment/remote-control flows.

## Cross-Cutting Implementation Baselines

Эти детали не являются отдельной product feature, но должны быть заданы до
активной реализации. Иначе Core, Node and Web начнут принимать локальные
решения, которые потом придется выравнивать.

### Trusted Development Security Baseline

`0.1` не является production security release, но у trusted/dev profile тоже
должны быть явные границы.

Minimum:

- Core default bind address is loopback unless user explicitly enables remote
  access.
- Node Daemon stores development credential outside repository/workspace paths.
- pairing code is short-lived and never reused as node credential.
- node credential never appears in URL, query string, UI copy, logs or event
  payloads.
- Core stores only node credential hash or equivalent verification material.
- revoke immediately rejects heartbeat and control channel attempts from that
  node.
- local HTTP/WS is allowed only for trusted loopback/dev profile; remote profile
  must show explicit non-production warning until security baseline ships.
- browser API should reject unknown origins in remote/dev-cross-origin cases
  where practical.
- logs redact bearer tokens, pairing codes, environment variables and provider
  secrets.
- UI labels all pairing and remote-control surfaces as trusted/local or
  controlled-development, not production-secure.

Explicitly not in `0.1`:

- team auth/RBAC;
- hardened browser login/session model;
- keychain-backed credential storage;
- credential rotation UX;
- mTLS/request signing;
- secrets management.

### API Error And Compatibility Contract

All Core APIs should return a consistent error envelope:

```text
error_code
message
details optional
retryable
correlation_id
```

Rules:

- `message` is user-safe.
- `details` is structured and safe to show in diagnostics when appropriate.
- raw provider/node errors are captured only in redacted debug fields/logs.
- every request gets a correlation id and it flows into commands/events where
  possible.
- command APIs return `command_id` when a durable command was recorded, even if
  later dispatch is pending or blocked.
- API and control frames include protocol/API version.
- breaking DTO changes require updating shared examples and Web protocol
  fixtures in the same change.

Initial error code families:

- `validation.*`
- `node.*`
- `placement.*`
- `runtime.*`
- `provider.*`
- `command.*`
- `event_stream.*`
- `auth_dev.*`
- `internal.*`

### Configuration And Local Operations

The first implementation should have explicit config, even if the defaults are
simple.

Core config:

- bind host/port;
- database path;
- web static assets path;
- deployment profile: `local_trusted` or `controlled_dev`;
- heartbeat stale/offline thresholds;
- optional stale-node UI sleep hint policy;
- control channel timeout;
- log level.

Node config:

- Core URL;
- node display name;
- credential path;
- allowed workspace roots or explicit paths;
- heartbeat interval;
- optional OS/provider sleep and wake hint reporting where practical;
- provider binary/path and provider-specific environment allowlist;
- local state directory;
- log level.

Developer commands/runbook should cover:

- start Core;
- start Web in dev mode or serve built assets;
- start Node and enroll it;
- register workspace placement;
- start fake-provider session;
- start Codex-provider session where Codex is available;
- inspect logs and database path;
- reset local dev state intentionally.

### Persistence And Migration Policy

SQLite is the durable truth for Core in `0.1`; migrations must be boring and
repeatable.

Rules:

- migrations run automatically on Core startup in local/dev profile;
- schema version is visible in diagnostics;
- empty database bootstraps without manual steps;
- migrations are append-only after a release tag;
- tests cover empty database and at least one previous dev snapshot where
  practical;
- repository layer owns all SQL access;
- projections can be rebuilt from durable rows/events;
- database path is explicit in config and logs at startup;
- deleting/resetting dev database is a deliberate documented action, not a
  hidden side effect.

Suggested SQLite pragmas for local/dev:

- `foreign_keys = ON`;
- WAL mode where practical;
- busy timeout configured;
- timestamps stored consistently as UTC.

### Observability And Diagnostics

V01 needs enough observability to debug distributed behavior without turning UI
into raw logs.

Structured log fields:

- `correlation_id`;
- `command_id`;
- `event_id`;
- `node_id`;
- `session_thread_id`;
- `runtime_session_id`;
- `project_placement_id`;
- `provider`;
- `control_channel_id`.

Core spans/events:

- enrollment created/approved/claimed/revoked;
- heartbeat accepted/rejected;
- control channel open/close/lost;
- command recorded/dispatched/acknowledged/completed/failed/blocked;
- event batch received/accepted/rejected/acked;
- projection gap/degraded state;
- runtime lifecycle transition;
- API error with safe code.

Node spans/events:

- config loaded;
- enrollment request/claim;
- heartbeat send/result;
- control channel connect/reconnect;
- command dedupe hit/miss;
- provider launch/resume/interrupt/stop;
- event outbox enqueue/ack/retry/drop;
- workspace validation/resource snapshot.

Diagnostics endpoints should return safe summaries, not secrets or raw
environment dumps.

### Provider Process Supervision

Provider Adapter implementation must define process behavior, not only method
names.

Minimum:

- launch command and cwd are explicit and logged in redacted form;
- provider process has lifecycle owner in Node runtime manager;
- stdout/stderr/protocol stream parsing is separated from event normalization;
- interrupt and stop have timeout-based escalation;
- process exit maps to `runtime.stopped`, `runtime.error` or
  `turn.interrupted` based on current state;
- provider startup failure maps to user-safe `provider.start_failed`;
- missing binary maps to `provider.missing_binary`;
- unsupported resume maps to degraded resume, not lost history;
- backpressure is bounded for provider output and event outbox;
- raw provider payload retention is explicit and redacted.

### Release Runbook Baseline

Before `0.1` handoff there should be one documented golden-path runbook.

It should include:

- prerequisites;
- exact commands;
- default ports and database/local-state paths;
- fake provider path;
- Codex provider path if available;
- expected UI states;
- common failures and recovery;
- how to collect safe diagnostics;
- known limitations and non-goals.

## Milestones

### M0. Repo And Tooling Scaffold

Цель: превратить documentation-only repository в buildable multi-app workspace,
пока еще без продуктового поведения.

Tasks:

- Создать Rust workspace scaffold.
- Добавить initial crates для shared protocol/domain, Core server, Node daemon
  и client helpers, если они нужны.
- Создать `apps/web` with Vite, React, TypeScript, Tailwind CSS v4 and baseline
  shadcn/ui conventions.
- Заложить `apps/web/src` structure from Web UI Implementation Blueprint:
  `app`, `shared`, `features` and `workbench`.
- Добавить frontend protocol types manually first, with a clear path to
  generated types later.
- Добавить example config files or documented defaults for Core and Node.
- Обновить `Makefile` только там, где нужно запускать реальные checks при
  наличии manifests.
- Добавить formatting, lint and test defaults для Rust and web.
- Добавить минимальные README run instructions для local development.

Acceptance:

- `make help` показывает runnable targets.
- `make c` проходит в fresh checkout после установки dependencies.
- Rust and web scaffolds build without placeholder failures.

### M1. Protocol And Domain Foundation

Цель: определить общий язык до того, как Core, Node and UI разъедутся.

Tasks:

- Определить typed ids для node, project, placement, session thread, runtime
  session, turn, message, command and event.
- Определить lifecycle enums для node, workspace placement, session thread,
  runtime session, turn, command, approval and warning severity.
- Определить command and event envelopes.
- Определить control channel frame types: hello, command dispatch/ack/result,
  event batch/ack, ping/pong and control error.
- Определить API error envelope and initial error code families.
- Определить correlation id propagation fields for APIs, commands, events and
  logs.
- Определить state transition helpers or validators for command, runtime,
  session thread and turn states.
- Определить API DTOs для snapshots: nodes, inventory tree, project placements,
  session thread, session messages, runtime state, event stream resume cursor.
- Определить UI-facing DTOs for timeline blocks, resource badges, references,
  lifecycle controls and command ids.
- Определить `UiBlock`, `BlockRendererRegistration`, `ArtifactTreeNode` and
  `AgentProjection` DTOs.
- Добавить serialization tests для representative commands/events.
- Добавить compatibility examples for HTTP snapshots, SSE event payloads and
  control channel frames.
- Добавить conversion boundaries between persistence rows and API/domain types.

Acceptance:

- Core and Node depend on the same protocol/domain crate.
- Command/event examples round-trip through JSON.
- Control frame examples round-trip through JSON.
- Error envelope examples round-trip through JSON.
- Block, artifact tree and agent projection examples round-trip through JSON.
- Domain types encode `V01` invariants instead of a generic chat transcript.

### M2. Core Server Skeleton And Persistence

Цель: сделать Core durable local control plane.

Tasks:

- Реализовать Axum server with config loading, tracing, health endpoint and API
  version endpoint.
- Добавить SQLite migrations для baseline tables.
- Добавить migration runner, schema version diagnostics and baseline SQLite
  pragmas.
- Добавить repository layer для nodes, enrollments, placements, sessions,
  commands, events, messages and approvals.
- Реализовать client-facing Core endpoints from API Surface Baseline with fake
  or read-only responses where later milestones fill behavior.
- Реализовать node-facing enrollment, heartbeat and control-channel endpoint
  skeletons.
- Реализовать event append with dedupe by `event_id`.
- Реализовать per-runtime sequence validation and gap marking.
- Реализовать status projections from persisted state.
- Реализовать read model builders for inventory, node detail, placement detail,
  session detail and runtime summary.
- Реализовать read model builders for session artifact tree and agent
  projection.
- Реализовать SSE session stream over persisted events with resume cursor.
- Реализовать consistent API error envelope with correlation ids.
- Добавить structured tracing fields for node, command, event, session,
  runtime and control channel ids.
- Добавить local development profile and visible API flag for non-production
  mode.

Acceptance:

- Core стартует с пустой SQLite database и применяет migrations.
- Core restart сохраняет nodes, placements, sessions, messages, commands and
  events.
- Unit/integration tests покрывают event append, command recording and
  projection reconstruction.
- Artifact tree and agent projection can be rebuilt after Core restart.
- Session stream can resume after a cursor or force snapshot reload on detected
  gap.
- API errors use the shared envelope and include a safe correlation id.

### M3. Node Enrollment And Heartbeat

Цель: сделать Node видимой в Core без inbound access to Node.

Tasks:

- Реализовать Node daemon CLI/config with Core URL, display name, workspace roots
  or explicit paths and local config file.
- Реализовать development enrollment request and pairing code flow.
- Реализовать Core endpoints для creating, approving, revoking and listing node
  enrollments.
- Хранить node credential locally on Node outside repository/workspace paths
  and credential hash/metadata in Core.
- Добавить redaction for pairing codes, bearer tokens and provider-sensitive
  fields in logs/diagnostics.
- Реализовать heartbeat from Node to Core with health, version, capabilities,
  activity summary and last known workspace summary.
- Реализовать reachable idle/active/degraded projection from fresh heartbeat
  plus active runtime/resource summary.
- Реализовать stale/offline projection based on missed heartbeat.
- Добавить optional sleep/wake hint reporting where OS/provider support is
  practical, with unknown as the default.
- Добавить basic node diagnostics endpoint and UI-ready snapshot.

Acceptance:

- User can start Core, start Node, approve pairing and see the node as
  reachable idle.
- Starting/stopping runtime changes activity summary without changing daemon
  presence.
- Stopping or sleeping Node moves it through stale/offline states without
  deleting it; UI may show sleep hint when available.
- Revoked node cannot continue heartbeat/control operations.
- Pairing code expires, is not reused as credential and is not logged.
- Heartbeat remains presence/config/channel negotiation, not command transport.

### M4. Workspace Placement And Resource Signals

Цель: дать пользователю bind work к concrete node-local workspace.

Tasks:

- Реализовать explicit workspace registration or validation request.
- Добавить `Project` and `ProjectPlacement` APIs.
- Node validates path existence, writability and allowed root boundary.
- Node gathers minimal git snapshot: repo URL/id, branch, commit, dirty, ahead,
  behind, untracked count where available.
- Core computes resource badges: offline, stale, missing workspace, read-only
  workspace, dirty, same branch active, same workspace active, behind remote,
  missing capability.
- Реализовать warning acknowledgement event.

Acceptance:

- UI/API can show `Nodes -> Projects/Workspaces`.
- Starting work on missing or unwritable workspace hard-blocks.
- Dirty or same-branch-active workspace warns and allows explicit override.
- Warning override is persisted as an event.

### M5. Core-To-Node Control Channel

Цель: перейти от presence к routed commands and ordered events.

Tasks:

- Реализовать outbound WebSocket/control channel opened by Node.
- Реализовать versioned JSON control frames from the blueprint.
- Добавить heartbeat response flag for `open_control_channel`.
- Реализовать Core command dispatch over active channel.
- Реализовать pending dispatch behavior when channel is temporarily closed.
- Реализовать Node command deduplication by `command_id`.
- Реализовать `command.ack` and `command.result` frame handling.
- Реализовать Node-to-Core event streaming over the control channel.
- Реализовать `event_batch` and `event_batch.ack`.
- Реализовать bounded Node local event outbox for unacked events.
- Persist all command intents and event results in Core.
- Добавить tests for duplicate command handling, event dedupe, seq gap marking
  and reconnect behavior.

Acceptance:

- Core can request a channel through heartbeat and dispatch a command when Node
  connects.
- Duplicate command delivery does not duplicate turns or runtime actions.
- Ordered events update Core projection and browser stream.
- Unacked events are retried after reconnect and deduplicated by Core.
- Seq conflict/gap produces degraded projection instead of silent corruption.
- Historical session state remains readable when Node disconnects.

### M6. Runtime Lifecycle With Fake Provider

Цель: доказать full lifecycle до того, как product correctness станет зависеть
от неопределенности Codex adapter.

Tasks:

- Определить minimal Provider Adapter trait для launch, resume, send turn,
  approval response, interrupt, stop and event stream.
- Добавить runtime manager that owns provider handles, runtime state and
  command-to-provider dispatch.
- Реализовать fake provider adapter that streams deterministic output,
  approvals, errors and completion events.
- Реализовать `StartRuntime`, `SendTurn`, `ResolveApproval`,
  `InterruptRuntime`, `StopRuntime` and `ResumeRuntime` through fake adapter.
- Реализовать runtime states: `starting`, `ready`, `running`, `blocked`,
  `expired`, `resuming`, `stopped`, `error`, `stale`.
- Реализовать `last_runtime_step_at` and 24h no-steps expiry behavior with a
  configurable test interval.
- Persist provider resume reference shape even if fake provider uses dummy data.
- Emit timeline-ready events for provider deltas, completed messages,
  approvals, lifecycle transitions and errors.
- Use `AgentProjection.resume_context` for fake degraded-resume tests.

Acceptance:

- Full session lifecycle works end to end with fake provider.
- UI/API can show running, blocked, interrupted, stopped, expired and resuming.
- Reloading Core/UI reconstructs session state from persistence.
- Approval request/resolve works through Core-routed commands and normalized
  events.
- Agent projection updates after turns, approvals and runtime state changes.
- Expired runtime can be resumed or shown as degraded when resume is impossible.

### M7. Codex Provider Adapter

Цель: заменить fake provider первым production adapter без утечки
Codex-specific details в Core or UI.

Tasks:

- Провести короткий protocol spike for Codex launch, streaming, resume,
  interrupt, stop, approvals and provider session identity.
- Реализовать Codex adapter behind the Provider Adapter boundary.
- Реализовать provider process supervision: launch cwd/env, stream parser,
  timeout-based interrupt/stop escalation and process exit mapping.
- Normalize Codex output into `provider.output.delta` and completed assistant
  messages.
- Normalize provider approval/user-input requests into Cortex approval events.
- Capture provider session id or resume cursor when available.
- Реализовать safe error mapping for provider startup, missing binary,
  unsupported resume, interrupted process and provider crash.
- Bound provider output buffering and raw payload retention.
- Keep fake provider available for tests and local demos without Codex.

Acceptance:

- User can start a Codex-backed session in a registered workspace.
- User can send multiple turns into the same live runtime while it is active.
- Provider output streams through Core and appears in the Web Control Panel.
- Stop and interrupt work for supported Codex process behavior.
- Resume works where provider support exists; otherwise UI shows degraded resume
  without losing thread history.
- Provider startup, missing binary, crash and unsupported resume produce
  user-safe error codes and redacted diagnostics.

### M8. Web Control Panel

Цель: поставить видимую product surface версии `0.1`.

Tasks:

- Build `AppShell` with top profile/status area, left inventory tree, main
  surface and right `InspectorStack` slot.
- Implement route map: `/nodes`, `/nodes/:nodeId`,
  `/nodes/:nodeId/placements/new`, `/placements/:placementId`,
  `/sessions/:sessionThreadId`, `/settings/runtime`.
- Implement `shared/api` HTTP client, SSE client and query keys.
- Implement shared API error parsing and user-safe diagnostics rendering.
- Implement TanStack Query hooks for inventory, nodes, placements, sessions,
  messages, artifact tree, agent projection and session event stream.
- Implement workbench reference model and `CortexRef` helpers.
- Implement internal block registry and initial core block renderers:
  inventory tree, artifact tree, agent chat, user message, assistant message,
  provider stream, runtime event, warning, error, approval request, workspace
  validation, resource snapshot, agent projection summary and unknown.
- Implement command registry and bind toolbar/block actions to Core mutations:
  node enrollment, revoke, placement validation, session start, send turn,
  attach, detach, interrupt, stop, resume, approval resolve and warning
  acknowledgement.
- Implement node list/detail, add/pair node flow, revoke action, diagnostics,
  heartbeat age and status badges.
- Implement project/workspace placement form, validation state and resource
  badge list.
- Implement session list and session route.
- Implement session header with node, project/workspace, provider, runtime
  status and warning badges.
- Implement lightweight `ArtifactTree` and `AgentProjectionPanel`.
- Implement `SessionTimeline`, `TimelineBlockRenderer`, `ChatComposer`,
  `LifecycleControls` and `ApprovalCard`.
- Implement pure projection helpers for applying SSE events to query caches and
  invalidating snapshots on gaps, including artifact tree and agent projection
  invalidation.
- Add trusted/local/non-production warning in node pairing and remote-control
  surfaces.
- Display correlation ids in diagnostics/error details where useful without
  exposing secrets.
- Keep reserved layout space for future workspace/artifact/trace panels without
  implementing those panels.
- Add Vitest coverage for route rendering, projection helpers, block fallback,
  lifecycle control enablement and SSE gap handling.

Acceptance:

- User can complete the full `0.1` release contract from the browser.
- UI reload reconstructs state from Core snapshots and resumes event streams.
- Idle/active/degraded/stale/offline/error states are visible in tree and
  session header where relevant.
- Runtime events, warnings and approvals render as structured blocks, not as
  assistant prose.
- Artifact tree and agent projection exist as minimal scalable UI scaffolding.
- UI actions go through command registry and Core APIs, never direct Node
  access.
- Unknown block/event payloads have a safe fallback.
- Chat surface remains usable without pretending to be a full IDE.

### M9. End-To-End Hardening

Цель: сделать первую release достаточно надежной для actual local development
use.

Tasks:

- Add end-to-end smoke script or documented manual runbook for Core + Node + Web.
- Test single local node, two local nodes, idle node, active node, stale node,
  offline/sleeping node, Core restart, Node restart, UI reload, duplicate
  command, provider crash and degraded resume.
- Add structured logs around enrollment, heartbeat, command dispatch, runtime
  lifecycle and event append.
- Add user-safe error messages and raw debug fields where appropriate.
- Check database migrations from empty database and from previous dev snapshots.
- Verify token/pairing-code redaction in representative logs and diagnostics.
- Document config defaults, default ports, database path and Node local state
  path.
- Document known limitations and recovery steps.
- Run `make c` before handoff.

Acceptance:

- The documented golden path works from a clean checkout.
- Failure scenarios do not corrupt persisted session history.
- Diagnostics are useful without exposing credentials or provider secrets.
- All non-goals remain clearly outside the shipped surface.
- `make c` passes or has documented no-op behavior for unscaffolded stacks.

## Рекомендуемый порядок сборки

1. M0 repo/tooling scaffold.
2. M1 protocol/domain foundation.
3. M2 Core persistence and server skeleton.
4. M3 enrollment/heartbeat.
5. M4 workspace placement/resource signals.
6. M5 control channel.
7. M6 fake provider lifecycle.
8. M8 Web Control Panel read-only shell can start after M2; command/lifecycle
   parts should run in parallel with M6 after APIs stabilize.
9. M7 Codex provider adapter.
10. M9 hardening and release gate.

Fake provider должен появиться до Codex adapter. Это снижает integration risk и
дает Core/UI стабильный contract for tests.

## Parallel Work Tracks

После M1 можно вести параллельно:

- Core persistence/API implementation.
- Node daemon heartbeat/control implementation.
- Web shell, workbench block/reference/command infrastructure and read-only
  snapshots.
- Provider adapter spike.
- Test harness and local runbook.

Не стоит параллелить API shape changes после M5 без явного contract update:
иначе Core, Node and Web начнут расходиться.

## Quality Gates

Каждый milestone должен оставлять repository in runnable state.

Обязательно перед handoff `0.1`:

- `make c`
- Rust formatting and clippy with warnings denied where Rust workspace exists.
- Rust unit/integration tests for domain, persistence, command/event logic and
  provider adapter behavior.
- Web lint, typecheck, build and component/unit tests where web app exists.
- Web projection/block/command registry tests for the V01 UI contract.
- Artifact tree and agent projection read-model tests.
- API error envelope, migration runner and config parsing tests where the
  relevant stack exists.
- Manual or automated golden-path E2E for Core + Node + Web + fake provider.
- Manual or automated golden-path E2E for Core + Node + Web + Codex provider,
  если Codex доступен в environment.

## Release Gate Checklist

`0.1` можно tag только когда:

- Release contract satisfied end to end.
- Trusted/local/non-production profile visible and documented.
- Trusted/dev security baseline is implemented: loopback default, credential
  redaction, pairing-code expiry and revoke enforcement.
- Persistent Core state survives restart.
- SQLite migrations are repeatable from empty database and documented previous
  dev snapshots.
- Node idle/active/degraded/stale/offline behavior visible and does not destroy
  history.
- Session history, messages, runtime events and provider resume reference stored
  in Core.
- Command idempotency prevents duplicate turns during retry/reconnect.
- Event ordering is monotonic per runtime session.
- Control channel reconnect retries unacked events without duplicating UI
  projection.
- Codex-specific behavior contained inside provider adapter.
- Web Control Panel does not depend on direct Node access.
- Web Control Panel uses internal surfaces, blocks, references and command
  registry for the session UI.
- Web Control Panel has lightweight artifact tree and agent projection surfaces
  without shipping the full artifact system.
- Runtime events, warnings, approvals and errors are visible as structured UI
  state outside assistant prose.
- User-safe API error envelope and correlation ids are used across Core/Web and
  relevant Core/Node paths.
- Structured logs cover enrollment, heartbeat, control channel, command,
  event batch and runtime lifecycle paths.
- Known limitations documented.
- `make c` passes.

## Main Risks And Mitigations

| Risk | Mitigation |
| --- | --- |
| Codex persistent protocol weaker than desired runtime model | Build fake provider first, keep degraded resume explicit, isolate Codex specifics behind Provider Adapter |
| Core/Node/Web contracts drift during implementation | Land shared protocol crate before feature work and require JSON round-trip tests |
| Web UI grows into future workbench scope | Keep `0.1` to control panel, chat, lifecycle, status and reserved layout only |
| UI modularity becomes over-engineered before plugins exist | Implement internal registries only for V01 blocks, refs and commands; no external plugin runtime |
| Artifact tree expands into full artifact product too early | Ship only session-local artifact/ref index and safe placeholders in 0.1 |
| Security expectations become ambiguous | Ship only trusted/local/controlled-development profile with visible warning and no production-security claim |
| Trusted/dev path leaks credentials through logs or diagnostics | Redact pairing codes, tokens, env and provider-sensitive values from the first implementation |
| Event log becomes raw text dump | Normalize minimum runtime/message/approval/error events from the start |
| Reconnect/retry creates duplicate turns | Make `command_id` mandatory and test Node command dedupe |
| Channel reconnect loses live provider output | Add bounded Node event outbox and Core event batch acknowledgements |
| Error handling fragments across Core, Node and Web | Use one error envelope, stable code families and correlation ids |
| Config and local state become implicit | Document defaults, paths and reset flow in the runbook before release handoff |
| Idle nodes look broken or inactive work looks like daemon failure | Split node reachability, activity summary and runtime lifecycle in protocol and UI |
| Stale/offline state loses user trust | Keep historical sessions readable and show last known state, heartbeat age, sleep hint when available and blocked commands clearly |

## Post-0.1 Follow-Up Candidates

После shipping `0.1` эти направления должны вернуться в feature queue:

- security baseline;
- runtime/session hardening;
- workspace shell and reference model;
- read-only Project Workspace Inspector;
- workspace intervention layer;
- causality and trace UX;
- git/review basics;
- Tool Registry v1.
